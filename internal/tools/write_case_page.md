Write the narrative case page.

Provide structured `summary` and `hypotheses` claims. Every claim must either
cite one or more evidence IDs (from `record_evidence`) or set `hypothesis: true`.
The Evidence table and Actions sections are filled automatically from what you
recorded during the run.

If any claim is unsupported, the tool returns the offending claims and does not
write the page — fix them and call it again.
