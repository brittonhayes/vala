# Tool classification and per-environment deny lists.
#
# `classes` assigns every tool a class. The class controls which phase a tool is
# exposed in (see internal/governance/phase.go). Tools NOT listed here default
# to the most restricted class (action_execute) so they fail closed.
#
# Read-only tools discovered from MCP evidence servers (e.g. Scanner's
# scanner_execute_query / scanner_load_context) are promoted into the read class
# at startup, so they are not listed here.
#
# `environments[*].deny` hard-denies tools in an environment regardless of phase
# or approval.
classes:
  read:
    - read
    - ls
    - glob
    - grep
    - reference_detection
    - validate_detection
    - test_detection
    - recall
  case_write:
    - open_case
    - record_evidence
    - write_case_page
    - queue_hunt
    - open_hunt
    - record_finding
    - store_hunt
    - record_intel
    - link_artifacts
  control:
    - propose_action
    - submit_for_approval
  action_execute:
    - slack_notify
    - bash
    - write
    - edit
    - ntn

environments:
  dev:
    deny: []
  prod:
    deny:
      - bash
      - write
      - edit
      - ntn
