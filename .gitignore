# Compiled binary (built with `go build -o vala ./cmd/vala`)
/vala
vala.exe

# Build / release output
/dist/

# Go test & coverage artifacts
*.test
*.out
coverage.txt

# Local editor / agent state
/.claude/
.idea/
.vscode/
*.swp
.DS_Store

# Local vala config & session transcripts (if ever placed in-tree)
.vala.json
/sessions/
